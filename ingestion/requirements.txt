# NLS ingestion pipeline (runs locally on the builder's machine, not deployed).
# Python 3.11+ recommended.

# DB
psycopg[binary]>=3.2

# Text extraction
pdfplumber>=0.11
python-docx>=1.1
python-pptx>=1.0

# OCR for scanned PDFs (also requires the Tesseract binary + poppler installed
# on the machine — see ingestion/README.md).
pytesseract>=0.3.13
pdf2image>=1.17
Pillow>=10.4

# LLM (course classification + MCQ extraction)
anthropic>=0.40

# Utilities
python-dotenv>=1.0
tqdm>=4.66
